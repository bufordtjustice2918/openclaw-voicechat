# Receiver config
OPENCLAW_TOKEN=change-me
INBOUND_DIR=../inbound

# OpenClaw workspace (for safe relative MEDIA paths)
WORKSPACE_DIR=/home/kavan/.openclaw/workspace

# Optional: delete file only after successful enqueue
DELETE_ON_SUCCESS=true
